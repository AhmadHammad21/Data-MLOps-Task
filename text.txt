


Install mysql and mysql workbench

Open the mysql CLI

1. Run this command to create a database with tables in mysql
source C:\Users\ahmed\OneDrive\Projects\Data-MLOps-Task\0_create_db_tables.sql


2. Test a sample data (Optional)
python insert_sample_data.py 

conda create --name de_env python=3.12 -y


conda create --name mlops_env python=3.12 -y

Naviage to the `model_deployment` directory

pip install -r requirements.txt

There are 3 ways to run the flask application

You can run the app.py flask and run the test.py
python app.py
test.py

or use dockerfiler first you need to build the image
docker build -t recommend_app .
docker run -p 5000:5000 recommend_app

then run python test.py
or send a request using postman


third method
Build and run the docker compose
docker-compose up --build

then run python test.py

I let the .env file there so you can run everything smoothly without setting up
databases credentials